<phone:PhoneApplicationPage
    x:Class="Dieta.View.CadastroUsuario"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Name="itemlPickerTemplate">
            <TextBlock x:Name="textBlock"
                       Text="{Binding}"
                       FontSize="50"/>
        </DataTemplate>
        <DataTemplate x:Name="itemLPickerTemplateModoCheio">
            <TextBlock Text="{Binding}" 
                       Margin="0,19,0,19"
                       FontSize="{StaticResource PhoneFontSizeLarge}"
                       FontFamily="{StaticResource PhoneFontFamilySemiLight}"/>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="#DIETA" 
                       Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="cadastro" 
                       Margin="9,-7,0,0" 
                       Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <ScrollViewer Height="500"  x:Name="ContentPanel"  Grid.Row="1" Margin="10,12,14,97">
            <StackPanel>
                <StackPanel Margin="0,6">
                    <TextBlock Text="Nome:" Width="430"/>
                    <TextBox x:Name="tBoxNome" Width="auto"/>
                </StackPanel>

                <StackPanel Margin="0,6">
                    <TextBlock Text="Sexo: " Width="430"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Name="rButtonMasculino" Content="Masculino"/>
                        <RadioButton Name="rButtonFeminino" Content="Feminino"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Margin="0,6">
                    <TextBlock Text="Idade:" Width="430"/>
                    <TextBox Name="tBoxIdade" 
                             InputScope="NumericPassword"/>
                </StackPanel>

                <StackPanel Margin="0,6">
                    <TextBlock Text="Peso: " Width="430"/>
                    <TextBox Name="tBoxPeso"
                             InputScope="NumericPassword"/>

                </StackPanel>

                <StackPanel Margin="0,6">
                    <TextBlock Text="Altura(m):" Width="430"/>

                    <TextBox Name="tBoxAltura" 
                             InputScope="NumericPassword"/>
                </StackPanel>

                <StackPanel Name="sPanelLPickerAtividade" Margin="0,12">
                    <TextBlock Text="Nível de atividade:" Width="430"/>
                    <toolkit:ListPicker Name="lPickerAtividade" 
                                        ItemTemplate="{Binding itemLPickerTemplate}"
                                        FullModeItemTemplate="{StaticResource itemLPickerTemplateModoCheio}"/>
                </StackPanel>

                <StackPanel Margin="0,6">
                    <TextBlock Text="Meta:" Width="430"/>
                    <toolkit:ListPicker Name="lPickerMeta" 
                                        ItemTemplate="{Binding itemLPickerTemplate}"
                                        FullModeItemTemplate="{StaticResource itemLPickerTemplateModoCheio}"
                                        />
                </StackPanel>

                <StackPanel Margin="0,6">
                    <TextBlock Text="Peso desejado:" Width="430"/>
                    <TextBox Name="tBoxPesoDesejado" InputScope="NumericPassword"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" 
                              IsMenuEnabled="True" 
                              Opacity="0.9">
            <shell:ApplicationBarIconButton IconUri="/Toolkit.Content/ApplicationBar.Check.png"
                                            Text="cadastrar" 
                                            Click="ApplicationBarIconButton_Click"/>

        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>